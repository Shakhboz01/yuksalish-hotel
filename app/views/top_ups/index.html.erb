<div class="container my-5">
  <div class="card shadow">
    <div class="card-body">
      <%= render 'top_ups', top_ups: @top_ups, total_price: @all_top_ups.sum(:price) %>
      <%= paginate @top_ups %>
    </div>
  </div>
</div>
